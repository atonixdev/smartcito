# ============================================================================
# File: citosmart/app/__init__.py
# Purpose:
#   Marks `app` as a Python package. Orca uses an explicit `app` package
#   so the FastAPI application can be imported as `app.main:app`.
# ============================================================================

__version__ = "0.1.0"
