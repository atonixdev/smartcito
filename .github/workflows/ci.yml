# ============================================================================
# File: .github/workflows/ci.yml
# Purpose:
#   Continuous Integration for SmartCito.
#     - Lints + tests the Python citosmart.
#     - Lints + builds the React webapp.
#   Triggered on every PR and on pushes to `main`.
# ============================================================================

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  citosmart:
    name: Citosmart (FastAPI)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: citosmart
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: citosmart/requirements-dev.txt

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Ruff
        run: ruff check .

      - name: Black (check only)
        run: black --check .

      - name: Mypy
        run: mypy app
        continue-on-error: true   # ratchet up to required once codebase grows

      - name: Pytest
        run: pytest

  webapp:
    name: Webapp (React)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: webapp
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: webapp/package-lock.json

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Lint
        run: npm run lint
        continue-on-error: true

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build
